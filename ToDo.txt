
? ReInitializeGeometry() or GeometryHasBeenModified() ?
- ascii model reimport bugs
- building material from ascii (using tgb line processer as refernce)
* import spectrums from ascii ( custom way )
    - translate G4propertiesvector to material property table ( design ascii format)( maybe still using custom line processor )
    * test the feature above
* Clean up the code ( and git branch as well).
* Make a standard library.

    